<UserControl x:Class="Test.Views.TestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Test.Views"
      mc:Ignorable="d"  
      d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel>
        <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
            <Button Content="Choose image" Command="{Binding GetImageButtonCommand}" Margin="10"/>
            <Image Source="{Binding SelectedImage}" Width="300" Height="300" Margin="10"/>
        </StackPanel>

        <Border DockPanel.Dock="Bottom" BorderBrush="#CCCCCC" BorderThickness="1" 
                Height="200" Margin="10"
                VerticalAlignment="Bottom">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F0F0F0">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="Python Server Log" Margin="10,4" />
                    </DockPanel>
                </Border>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBox Grid.Row="1" 
                             Text="{Binding OutputText, Mode=OneWay}" 
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </DockPanel>



</UserControl>
